# FRD CEO Pulse PWA

Upload the files in this folder to the root of the `frd-ceo-pulse` GitHub repository.

Then enable GitHub Pages: Settings → Pages → Deploy from a branch → `main` / `(root)`.

The frontend reads live data from the existing Google Apps Script deployment.
